[Pre-installed]
pdsh


[Start]
Configure>	modify conf/site.conf
Setup>		wrapper/setup.sh HDFS_BLOCK_SIZE JAVA_HEAP_SIZE MAPRED_OUT_DIR
Generate Data>	wrapper/dgen.sh WORKLOAD SCALE_FACTOR HDFS_PATH
	(WORKLOAD) ssb, tpch
Test>		wrapper/test.sh

